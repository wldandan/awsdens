<%= form_for(@artifact_type_template, :html => { :class => "form-horizontal"}) do |f| %>
  <% if @artifact_type_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artifact_type_template.errors.count, "error") %> prohibited this artifact_type_template from being saved:</h2>

      <ul>
      <% @artifact_type_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, "项目名称:",:class=>"control-label" %>
    <div class="controls"> <%= f.text_field :name %> </div>
  </div>
  <div class="control-group">
    <%= f.label :price, "项目价格:", :class=>"control-label"%>
    <div class="controls"> <%= f.number_field :price %> </div>
  </div>
  <div class="control-group">
    <%= f.label :description, "描述信息:", :class=>"control-label"%>
    <div class="controls"><%= f.text_area :description, :rows=>"3" %></div>
  </div>

  <div class="control-group">
    <div class="controls">
    <%= submit_tag "保存",:class => "btn btn-small"%>
    <%= link_to '取消', artifact_type_templates_path, :class=>'btn btn-small' %>
    </div>
  </div>

<% end %>
